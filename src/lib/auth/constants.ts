/** HttpOnly cookie carrying the opaque session token (matches `Session.token`). */
export const AUTH_SESSION_COOKIE = "session";

/** Session lifetime (30 days). */
export const SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 30;
